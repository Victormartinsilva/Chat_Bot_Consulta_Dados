# Exemplo de arquivo de secrets para Streamlit Cloud
# Copie este conteúdo para: Settings > Secrets no Streamlit Cloud

# Provedor LLM (gemini, openai, ou ollama)
LLM_PROVIDER = "gemini"

# Configuração do Google Gemini
GOOGLE_API_KEY = "sua_chave_gemini_aqui"
GEMINI_MODEL = "gemini-2.5-flash"

# Configuração do OpenAI (se usar OpenAI)
# OPENAI_API_KEY = "sua_chave_openai_aqui"
# OPENAI_MODEL = "gpt-3.5-turbo"

# Configuração do Ollama (se usar Ollama local)
# OLLAMA_MODEL = "llama3.2"
# OLLAMA_BASE_URL = "http://localhost:11434"

# URL do CSV (opcional - se quiser carregar de uma URL)
# CSV_URL = "https://exemplo.com/dados.csv"

